{
  "version": 2,
  "builds": [
    {
      "src": "build/api/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/qrcode", "dest": "/api/qrcode" },
    { "src": "/redirect", "dest": "/api/redirect" }
  ]
}
